eureka:
  instance:
    hostname: http://eureka-server-mukul-k.9a6d.starter-us-east-1a.openshiftapps.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
